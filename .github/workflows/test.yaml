name: Test

on: [push, pull_request]

jobs:
  hrval:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Validate Helm Releases in system namespace
        uses: stefanprodan/hrval-action@v3.2.0
        with:
          helmRelease: system/
          helmVersion: v3
